import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 255, 164, 36); // Laranja
  static const Color background =
      Color.fromARGB(255, 255, 227, 188); // Tom pastel (creme claro)
  static const Color textPrimary = Colors.black; // Preto para textos principais
  static const Color textSecondary =
      Color.fromARGB(255, 255, 164, 36); // Laranja para destaques
  static const Color shadow =
      Color.fromARGB(255, 81, 81, 81); // Laranja para destaques
}
